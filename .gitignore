#Ignora apenas arquivos/pasta que não foram enviadas ainda para o git
# git update-index --assume-unchanged arquivo-a-ser-ignorado  
# git update-index --no-assume-unchanged arquivo-a-ser-revertido
# git rm --cached -r suporte  (para pasta na raiz) e para expecificar git rm --cached caminho/para/teste.js
# git ls-files -v . | findstr "^H" (mostra todos arquivos que estão sendo atualizados)
# git ls-files --others --modified (mostra todos arquivos que estão sendo ignorados)
# git ls-files -v | Where-Object { $_.StartsWith('h') } | ForEach-Object { $_.Substring(2) }  (para ver todos arquivos ignorados)
# Get-ChildItem -Recurse | Where-Object { $_.FullName -notlike '*\node_modules\*' }   (mostrar estrutura do projeto)
# npm outdated (Mostras todas npm e atualizações)





